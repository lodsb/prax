{
  "mcpServers": {
    "prax": {
      "type": "stdio",
      "command": "${PRAX_PYTHON:-.venv/Scripts/python.exe}",
      "args": ["-m", "prax.mcp_server"],
      "env": {
        "PRAX_DOOR": "${PRAX_DOOR:-http://127.0.0.1:8000}",
        "PRAX_TOKEN": "${PRAX_TOKEN:-}"
      }
    }
  }
}
