# prax capture

The browser extension: "send this tab" and "send all tabs in window" to
a prax door. How to install and what it does: `docs/extension.md` in the
repository root.

## License

This directory is licensed under the GNU Affero General Public License
v3.0 or later (`LICENSE` here), because it bundles SingleFile
(`vendor/single-file/`, AGPL) to save pages as self-contained snapshots,
the way the Zotero connector does. The rest of prax, the server the
extension talks to over HTTP, is MIT (`LICENSE` in the repository root).
