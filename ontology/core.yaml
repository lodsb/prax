# prax ontology, core module: the types every domain shares. A domain
# module (research.yaml, studio.yaml, later family.yaml) requires
# this one and subtypes it: an author is a person, a paper is a document.
# Keep it SMALL; a subtype is accepted wherever its parent is allowed, so
# relations declared here on person hold for authors. Format:
# src/prax/ontology.py. Bump `version` when a type or relation changes.
#
# `naming:` says whether a type's names are `proper` (one particular
# thing: a person, a publisher, a product, a title — the same string in
# every language) or `common` (a kind of thing, which every language has
# its own word for). A type that declares nothing inherits from its
# parent, and a root that declares nothing is proper, because translating
# a name that should not be translated is the worse mistake. It is what
# tells the extraction prompt which names to write in English and the
# vocabulary pass which entities may be folded across languages. It is
# not a change to the type set — no triple that validated before stops
# validating — so it bumps no version and re-extracts nothing.
#
# core v1 (2026-09-12): split out of the single ontology.yaml (v5); the
# shared types the modular ontology needs, and the two relations that
# belong to people and organizations rather than to research.
# core v2 (2026-09-13, docs/ontology-v7.md): what every kind of document
# shares came here from the domain modules — `authored_by` (research had
# it, studio had `written_by` for the same thing) and `part_of`, widened
# to organizations (a lab in a university, a subsidiary in a group) —
# and two relations the review queue asked for: `located_in`
# (organization or event → place) and `published_by` (document →
# organization, the publisher or issuing body).
# core v3 (2026-09-22, docs/ontology-v8.md): affiliation is not only a
# person's — an organization is affiliated with a network, a parent body
# or a movement, and a person with a school of thought — so
# `affiliated_with` takes organizations on the left and concepts on the
# right; `located_in` takes a person ("based in Berlin"); and the
# relations the models keep saying the other way round are aliases that
# flip (`has_part`, `publisher_of`).
module: core
version: 3

entity_types:
  person:
    description: >-
      A human being named in the text, as printed, full form, no titles.
      Domain modules subtype this (an author is a person).
  organization:
    description: >-
      A university, department, lab, company, funder, consortium or
      standards body named in the text: Chalmers University of Technology,
      LMA-CNRS, Waves Audio, Austrian Science Fund. Full name as printed,
      no city or country unless part of the name.
  document:
    description: >-
      Something written or recorded that the store holds or the text names
      by title. Domain modules subtype this (a paper, a page).
  place:
    description: >-
      A named location: a city, a studio, a venue building, a country.
  event:
    description: >-
      A dated happening the text names: a conference edition, a concert,
      a release, a meeting.
  work:
    description: >-
      A creative work named as such: a piece of music, an album, a film,
      a book as a work rather than as the document at hand.
  concept:
    naming: common
    description: >-
      An idea, topic, phenomenon or quantity a document is about: source
      separation, pitch perception, aliasing, room acoustics. Lowercase
      noun phrase, singular, no articles. The field a technique belongs to
      is a concept; the technique itself is a method, never both under one
      name.
  tool:
    description: >-
      Software, hardware, a library or an instrument used or built:
      Matlab, Csound, Web Audio API, a specific synthesizer.

relation_types:
  affiliated_with:
    domain: [person, organization]
    range: [organization, concept]
    description: >-
      the person's institution as stated in the document; an
      organization's network, parent body or movement (a lab in a
      consortium, a label under a group, a studio of a school); a person
      or organization's school of thought or movement, as a concept.
      When one organization is inside the other, part_of is the relation
  developed_by:
    domain: [tool]
    range: [organization, person]
    description: who built or makes the tool, as the text states it
  authored_by:
    domain: [document]
    range: [person, organization]
    description: >-
      a listed author of the document (a paper's authors, a reviewer, a
      designer credited on a schematic); or the organization that issued
      it when no person is named (a standards body, a manufacturer's
      manual)
  published_by:
    domain: [document]
    range: [organization]
    description: >-
      the publisher or issuing body (Springer, O'Reilly, the manufacturer
      behind a manual); where it appeared — the journal, the conference —
      is published_in
  part_of:
    domain: [document, organization]
    range: [document, organization]
    description: >-
      a document within a larger one (a chapter in a book, an article in a
      proceedings volume, a paper on a project's reading list); or an
      organization within a larger one (a lab or department in a
      university, a subsidiary in a group)
  located_in:
    domain: [organization, event, person]
    range: [place]
    description: >-
      where an organization is based, an event was held or a person is
      based, as the text states it (a city, a country)

type_aliases:
  institution: organization
  university: organization
  company: organization
  people: person
  software: tool
  library: tool
  instrument: tool

relation_aliases:
  affiliation: affiliated_with
  written_by: authored_by
  author: authored_by
  author_of: authored_by
  authors: authored_by
  is_part_of: part_of
  publisher: published_by
  located_at: located_in
  based_in: located_in
  affiliated: affiliated_with
  created_by: developed_by
  built_by: developed_by
  made_by: developed_by
  member_of: affiliated_with
  publisher_of: {to: published_by, reversed: true}
  has_part: {to: part_of, reversed: true}
  contains: {to: part_of, reversed: true}
